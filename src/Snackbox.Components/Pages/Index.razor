@page "/"
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Redirect to the scanner home page
     //   Navigation.NavigateTo("/scan", forceLoad: true);
    }
}
