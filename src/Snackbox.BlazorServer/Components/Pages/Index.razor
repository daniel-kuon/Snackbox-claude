@page "/"
@inject NavigationManager Navigation

@code {
    protected override void OnAfterRender(bool firstRender)
    {
        if (firstRender)
        {
            // Redirect to login page for web app
            Navigation.NavigateTo("/login", replace: true);
        }
    }
}
